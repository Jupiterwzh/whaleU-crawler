# 待办.md — 延期/主线事项追踪

> 规则：分批处理任务时，未办项必记于此防遗忘（AGENTS.md 规则 37）。完成即勾掉。
> 最后更新：2026-08-12

---

## 当前主线：RAG 管理 Agent + 检索增强

### RAG 管理 Agent（已完成实现 + 真实验收 ✅）
- [x] 新建 `rag-manager/` 独立 Agent（真复制 harness 内核，同 query-agent 模式）
  - 工具：read_rag_docs / assign_validity / rebuild_index
  - 触发：每次数据库更新后自动调用（非 query 调用时）
- [x] shared/rag/ragstore.py 支持有效时间字段：
  - 文档写入 valid_from / valid_until / effective_days
  - current 索引只含仍有效（valid_until > 今天）的文档，过期自动排除进 archive
  - `pending_validity()` 查询待处理通知
- [x] query-agent run_crawler 入库后触发 rag_manager
- [x] 有效时间判定逻辑抽纯函数（时间戳链解析/effective_days 档位 15/30/60/90/180），确定性单测
- [x] **RAG 管理 Agent 真实验收通过**（2026-08-12）：98 条入库 → rag-manager 赋有效时间 98/98 → current 9 条/archive 98 条 → 查询命中

### RAG 检索能力增强（已实现 + 真实验收 ✅）
- [x] R1: search 支持 date_from / date_to 时间过滤
- [x] R2: search 支持 scope="archive" 历史检索入口
- [x] R3: search 支持 domain 过滤
- [x] L1: run_crawler 入库后触发 rag-manager（返回确认信息）
- [x] L2: 爬虫输出 jsonl 入库后归档（data/archive/，2026-08-13）
- [x] R4: search 返回正文片段截取（snippet 字段，命中词前后60字 + 工具层接入，2026-08-13）

### 全链路真实验收（✅ 2026-08-12 通过）
- [x] 爬虫真实爬取（cs.nju.edu.cn 98 条）
- [x] query-agent 完整查询（rag_search 命中 → 回答带 URL，不触发爬虫）
- [x] 三 Agent 统一 DeepSeek key（LLM_BASE_URL/api.deepseek.com/v1, DEEPSEEK_API_KEY, deepseek-v4-flash）

## 当前主线：策略 Agent 网站结构遍历

### 网站结构遍历（rules + crawl_structure 已实现）
- [x] 实现 rules 遍历指令（BFS + visited + 页面分类 + 防循环 + 外链停止 + 深度/链接上限）
- [x] crawl_structure 工具（策略 Agent 内 `structure_tools.py`，确定性 BFS 返回结构树）
- [x] 节点 index + title（锚文本）+ type（URL 与首页文字对应）
- [x] 用户选择交互（遍历后展示编号清单，输入编号选为入口）
- [x] 分类升级（多模式通知检测，URL list 后缀 + 内容阈值）
- [x] **首页预览追踪**：首页标记 home + preview_count（通知预览数），不作为列表页候选
- [x] **信息页处理**：锚文本含信息栏目关键词的非列表页标记 info，不深挖
- [x] **max_links 截断提示**：truncated 标志 + 重试更大 max_links
- [x] **列表页 category 自动标注**：notice/info（按锚文本，仅参考不默认选）
- [x] **关键词独立配置文件**：keywords.json（infoKeywords/noticeKeywords），可随时调整
- [ ] **待决定**：
  - 结构树是否保存为文件供复查（之前选否，仍否？）

### 爬虫/策略 Agent 改进（用户确认"先真实验收再改"）
- [x] 反馈闭环：爬虫 --verify + Agent 生成后验证（yzb 完整跑通）
- [x] 经验库：跨站点通用规律（CMS/踩坑/部门类型），探索注入 + 人工确认写入（2026-08-13 实现）
- [x] 经验库完善：Agent 自动提取本次新规律（【经验草案】JSON）→ 确认后去重合并进经验库（2026-08-13）
- [ ] **爬虫区分站内通知 vs 外链**：grawww 首页多为 mp.weixin.qq.com 外链，入库 RAG 前应甄别（是否保留外链待定）
- [ ] **ndwy.nju.edu.cn 需 VPN**：连接 VPN 时 Agent 进程无法访问，不连时站点不可达——需用户决定是否给 Agent 进程配 VPN，或跳过此类站点
- [ ] 域名异常站点：jwb（纪检监察网）、xkb（学科建设办）——docx 清单域名可能错，需核对
- [ ] hr/tzb/grawww 等站点批量生成策略（验证闭环泛化）

---

## 大作业工程收尾（延期）

- [ ] query-agent 真实验收（真实 LLM + 真实网站跑通"检索→不够→爬→回答"全流程，需用户 key）
- [ ] WebUI 部署到可访问地址（作业 §五.9，webui.py 已实现待部署）
- [ ] CI 实跑 + 末次 pass（push GitLab 触发 .gitlab-ci.yml 的 unit-test job）
- [ ] Docker build/run 验证（Dockerfile 已写，耗时命令待用户手动）
- [ ] 仓库推送 + PR 工作流说明（目前单分支 main）
