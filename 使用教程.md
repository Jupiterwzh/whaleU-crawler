# 自动信息检索模块 — 完整使用教程

> 南京大学智能搜索爬虫系统
> 版本：1.0.0 | 更新日期：2026-07-22

---

## 目录

1. [项目简介](#1-项目简介)
2. [快速开始](#2-快速开始)
3. [独立爬虫模块详解](#3-独立爬虫模块详解)
4. [完整系统（Agent）详解](#4-完整系统agent详解)
5. [数据接口说明](#5-数据接口说明)
6. [命令行工具一览](#6-命令行工具一览)
7. [站点清单](#7-站点清单)
8. [常见问题](#8-常见问题)

---

## 1. 项目简介

本项目是一套南京大学信息爬取系统，包含两套模块：

| 模块 | 路径 | 功能 | 依赖 |
|------|------|------|------|
| **独立爬虫模块** | `standalone/` | 浏览器登录 + 爬取 + 清洗 + 存储 | Node.js（+ 浏览器服务可选） |
| **完整系统** | `src/` | 爬取 + Agent 智能问答 + 智搜搜索 | Node.js + 浏览器服务 |

**130 个站点覆盖**：计算机学院、医学院、图书馆、行政部门等南京大学全部官方站点。

---

## 2. 快速开始

### 2.1 测试（无需配置）

```bash
cd standalone

# 测试HTTP模式（立即可用）
node collector.js --http --url https://cs.nju.edu.cn/

# 测试网站探索Agent（交互式）
node site-explorer.js --url https://jw.nju.edu.cn/
```

### 2.2 配置API Key（可选，需要AI能力时）

**支持的模式**：

| 模式 | 环境变量 | 模型 |
|------|----------|------|
| 本地模式（默认） | 无需配置 | 关键词匹配，无AI能力 |
| Claude | `LLM_MODE=claude` + `ANTHROPIC_API_KEY` | Claude Sonnet 4 |
| OpenAI | `LLM_MODE=openai` + `OPENAI_API_KEY` | GPT-4o-mini |

**配置步骤**：

```cmd
# 方式一：命令行临时配置（当前窗口有效）
cd D:\PY\lanling-u\standalone
set LLM_MODE=claude
set ANTHROPIC_API_KEY=sk-ant-your-key-here
```

```cmd
# 方式二：永久配置（Windows）
# 控制面板 → 系统 → 高级系统设置 → 环境变量 → 新建系统变量

# 变量名：LLM_MODE，值：claude
# 变量名：ANTHROPIC_API_KEY，值：sk-ant-your-key-here
```

**验证配置**：
```bash
node -e "const {CONFIG}=require('../src/agent/llm'); console.log('模式:', CONFIG.mode, 'Key已配置:', !!CONFIG.anthropicKey);"
```

### 2.3 选择模型

默认模型：
- Claude模式：`claude-sonnet-4-20250514`
- OpenAI模式：`gpt-4o-mini`

**自定义模型**：
```cmd
set LLM_MODEL=claude-opus-4-7
```

**推荐模型**：

| 用途 | Claude模型 | OpenAI模型 |
|------|-----------|------------|
| 快速测试 | `claude-haiku-4-5-20251001` | `gpt-4o-mini` |
| 平衡性能 | `claude-sonnet-4-20250514` | `gpt-4o` |
| 最佳效果 | `claude-opus-4-7` | `gpt-4o` |

### 2.4 构建Claude Code Harness

**Harness配置文件**：`settings.json`

位置：`D:\PY\lanling-u\standalone\.claude\settings.json`

```json
{
  "permissions": {
    "allow": [
      { "prompt": "run node commands", "tool": "Bash" },
      { "prompt": "read files in project", "tool": "Read" },
      { "prompt": "write files to project", "tool": "Write" }
    ]
  },
  "env": {
    "LLM_MODE": "claude",
    "ANTHROPIC_API_KEY": "env:ANTHROPIC_API_KEY"
  },
  "memory": {
    "enabled": true
  }
}
```

**启动Claude Code**：
```bash
cd D:\PY\lanling-u\standalone
# 方式一：指定项目目录
claude --project .

# 方式二：指定配置文件
claude --settings .claude/settings.json
```

### 2.5 .env文件使用说明

**⚠️ 重要：永远不要让AI读取.env文件**

`.env` 文件仅供你本地运行脚本时使用，不是给AI读取的。

```
.env                    # 你的本地配置（已gitignore）
.env.example           # 模板（会被git跟踪）
```

**正确流程**：
1. 复制 `.env.example` 为 `.env`
2. 编辑 `.env` 填入你的API Key
3. 运行脚本时通过环境变量传入：
```cmd
# Windows
set ANTHROPIC_API_KEY=sk-ant-xxx && node site-explorer.js --url https://xxx

# 或在BAT文件中
@echo off
set ANTHROPIC_API_KEY=sk-ant-xxx
node site-explorer.js --url https://xxx
```

### 2.3 如果你需要爬取数据

使用 `standalone/` 目录（独立爬虫模块）：

```bash
cd standalone

# 方式一：HTTP 模式（立即可用，无需配置）
node src/collectors/collector.js --http --url https://cs.nju.edu.cn/

# 方式二：浏览器模式（需要登录一次）
node scripts/browser-start.js   # 首次：启动浏览器并登录
node src/collectors/collector.js --all  # 爬取全部 130 个站点
```

### 如果你需要智能问答功能

使用 `src/` 目录（完整系统）：

1. 解压 `浏览器服务_skill.zip` 到合适位置（如 `C:\tools\skills-nju-browser`）
2. 配置环境变量 `NJU_BROWSER_SKILLS=C:\tools\skills-nju-browser`
3. 启动浏览器并登录
4. 运行 `node src\agent\index.js`

---

## 3. 独立爬虫模块详解

### 3.1 目录结构

```
standalone/
├── src/                          # 源代码
│   ├── collectors/               # 爬虫模块
│   │   └── collector.js         # 主入口
│   ├── explorers/                # 探索Agent模块
│   │   └── site-explorer.js     # 交互式网站探索
│   ├── analyzers/                # 分析器模块
│   │   ├── analyzer.js          # 启发式分析
│   │   └── agent-analyzer.js    # LLM分析器
│   ├── strategies/               # 策略管理模块
│   │   └── strategy-manager.js  # 策略CRUD
│   └── data/                     # 数据模块
│       └── sites.js             # 站点清单
├── browser/                      # 浏览器服务
├── data/                         # 数据目录
│   ├── records/                  # 爬取结果
│   │   └── all_records.jsonl
│   └── strategies/               # 策略文件
├── scripts/                      # 脚本
│   └── browser-start.js         # 启动浏览器
├── config/                       # 配置模板
│   └── .env.example
└── .claude/                      # Claude Code配置
```

### 3.2 两种爬取模式

| 模式 | 命令 | 登录 | JS渲染 | 速度 |
|------|------|------|--------|------|
| **HTTP 模式** | `--http` | 无需 | ❌ | 快 |
| **浏览器模式** | （默认） | 需要一次 | ✅ | 较慢 |

**HTTP 模式**示例：
```bash
node collector.js --http --url https://cs.nju.edu.cn/
node collector.js --http --all
```

**浏览器模式**示例：
```bash
node browser-start.js    # 仅首次需要
node collector.js --all
```

### 3.3 主要命令

```bash
# ===== 网站探索Agent（推荐首次使用）=====
# 交互式探索网站结构
node src/explorers/site-explorer.js --url https://jw.nju.edu.cn/

# 继续之前的探索
node src/explorers/site-explorer.js --continue jw.nju.edu.cn

# 列出进行中的探索
node src/explorers/site-explorer.js --list

# ===== 爬虫命令 =====

# 爬取单个页面
node src/collectors/collector.js --url https://cs.nju.edu.cn/
node src/collectors/collector.js --http --url https://cs.nju.edu.cn/

# 爬取全部 130 个站点
node src/collectors/collector.js --all
node src/collectors/collector.js --http --all

# 从文件读取 URL 列表
node src/collectors/collector.js --list urls.txt

# 查看数据统计
node src/collectors/collector.js --stats

# 显示帮助
node src/collectors/collector.js --help
```

### 3.4 首次使用流程

**步骤 1：测试 HTTP 模式（推荐先试这个）**
```bash
node collector.js --http --url https://cs.nju.edu.cn/
```
如果成功，说明 Node.js 环境正常，可以立即开始爬取。

**步骤 2（可选）：配置浏览器模式**

如果 HTTP 模式无法爬取某个页面（如需要 JavaScript 渲染），使用浏览器模式：
```bash
# 首次：配置环境变量（永久生效需要添加到系统环境变量）
set NJU_BROWSER_SKILLS=C:\tools\skills-nju-browser

# 启动浏览器并登录
node browser-start.js

# 爬取
node collector.js --all
```

### 3.5 数据输出

所有数据保存在 `standalone/data/` 目录：

| 文件 | 说明 |
|------|------|
| `all_records.jsonl` | 全量记录（自动去重，主文件） |
| 分次记录 | 每次爬取如指定文件名则保存到此 |

**查看数据量：**
```bash
node collector.js --stats
```

---

## 4. 完整系统（Agent）详解

### 4.1 目录结构

```
src/
├── agent/                  # AI Agent（自然语言问答）
│   ├── index.js          # Agent 主程序（交互入口）
│   └── llm.js           # LLM 接口（Claude/OpenAI/本地模式）
├── crawler-core.js       # 浏览器 API 引擎
├── cleaner.js            # 数据清洗
├── storage.js            # 数据存储
├── collector-generic.js  # 页面采集器（需浏览器服务）
├── collector-search.js   # 搜索采集器（需浏览器服务）
├── site-adapters/        # 站点适配器
├── sites.js             # 站点清单（130个）
└── run-crawl.js        # 一键全量采集
```

### 4.2 环境配置

**第一步：解压浏览器服务**

找到 `浏览器服务_skill.zip`，解压到合适位置（如 `C:\tools\`）。
解压后目录包含：`nju-browser-server.js`、`nju-browser-start.js` 等。

**第二步：配置环境变量**

Windows CMD（临时）：
```cmd
set NJU_BROWSER_SKILLS=C:\tools\skills-nju-browser
```

永久配置：系统属性 → 高级 → 环境变量 → 新建系统变量

**第三步：启动浏览器并登录**

```cmd
node %NJU_BROWSER_SKILLS%\nju-browser-start.js
```

首次需要完成统一身份认证登录（扫码或账号密码）。

**第四步：启动 Agent**

```cmd
cd <项目根目录>
node src\agent\index.js
```

### 4.3 Agent 使用示例

```
自动信息检索模块> 计算机学院
【计算机学院】http://cs.nju.edu.cn/
来源分类: 教学科研单位

相关内容片段：
...（页面内容摘要）...

自动信息检索模块> 医学院
【医学院】http://med.nju.edu.cn/
...

自动信息检索模块> 列出所有站点
📚 南京大学站点清单（共 130 个页面）：
...

自动信息检索模块> 帮助
```

### 4.4 LLM 模式配置（可选）

Agent 支持三种模式，通过环境变量 `LLM_MODE` 切换：

| 模式 | 设置 | 说明 |
|------|------|------|
| `local` | 默认 | 关键词匹配，无需 API Key |
| `claude` | `set LLM_MODE=claude` + `ANTHROPIC_API_KEY` | Claude 大语言模型 |
| `openai` | `set LLM_MODE=openai` + `OPENAI_API_KEY` | GPT 等 OpenAI 模型 |

**配置 Claude API：**
```cmd
set ANTHROPIC_API_KEY=sk-ant-xxxxx-your-key-here
set LLM_MODE=claude
node src\agent\index.js
```

---

## 5. 数据接口说明

### 5.1 数据格式

所有爬取结果统一保存为以下 JSON 格式：

```json
{
  "title": "计算机学院主页",
  "url": "https://cs.nju.edu.cn/",
  "content": "南京大学计算机学院主页，包含通知公告、师资介绍等内容...",
  "publishTime": null,
  "source": {
    "author": "",
    "department": "计算机学院",
    "siteName": "计算机学院"
  },
  "tags": ["通知公告", "招聘信息", "学术报告", "暑期活动", "获奖成果"],
  "crawler": "nju-crawler",
  "crawlTime": "2026-07-22T14:30:00.000Z"
}
```

### 5.2 在代码中读取数据

**独立爬虫模块：**
```javascript
const fs = require('fs');
const path = require('path');

const DATA_DIR = path.join(__dirname, 'standalone', 'data');
const files = fs.readdirSync(DATA_DIR).filter(f => f.endsWith('.jsonl'));

let allRecords = [];
files.forEach(file => {
  const content = fs.readFileSync(path.join(DATA_DIR, file), 'utf8');
  content.split('\n').filter(l => l.trim()).forEach(line => {
    allRecords.push(JSON.parse(line));
  });
});

console.log(`共 ${allRecords.length} 条记录`);
allRecords.forEach(r => console.log(r.title, r.url));
```

### 5.3 数据字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `title` | string | 页面标题 |
| `url` | string | 页面 URL |
| `content` | string | 页面正文（去 HTML 标签） |
| `publishTime` | string/null | 标准化日期（YYYY-MM-DD），未识别到则为 null |
| `source.department` | string | 推断的部门/学院名称 |
| `source.siteName` | string | 站点显示名称 |
| `tags` | string[] | 自动推断的标签（通知公告、学术报告等） |
| `crawler` | string | 固定值 "nju-crawler" |
| `crawlTime` | string | ISO 格式爬取时间 |

---

## 6. 命令行工具一览

### 独立爬虫模块（standalone/）

| 命令 | 功能 |
|------|------|
| `node collector.js --url <URL>` | 爬取单个页面（浏览器模式） |
| `node collector.js --http --url <URL>` | 爬取单个页面（HTTP 模式） |
| `node collector.js --all` | 爬取全部站点（浏览器模式） |
| `node collector.js --http --all` | 爬取全部站点（HTTP 模式） |
| `node collector.js --list <文件>` | 从文件读取 URL 列表 |
| `node collector.js --stats` | 查看数据统计 |
| `node browser-start.js` | 启动浏览器并等待登录 |
| 双击 `启动爬虫.bat` | 浏览器模式爬取全部（Windows） |
| 双击 `启动爬虫_HTTP.bat` | HTTP 模式爬取全部（Windows） |

### 完整系统（src/）

| 命令 | 功能 |
|------|------|
| `node src\agent\index.js` | 启动 Agent 交互模式 |
| `node src\collector-generic.js --url <URL>` | 爬取单个页面 |
| `node src\collector-generic.js --all` | 爬取全部站点 |
| `node src\collector-search.js --keyword <词> --max 20` | 搜索采集 |
| `node src\run-crawl.js` | 一键全量采集 |

---

## 7. 站点清单（共 130 个页面）

| 分类 | 数量 | 示例 |
|------|------|------|
| 学校概况 | 5 | 南大简介、现任领导、历任领导 |
| 党群组织 | 16 | 党委办、纪委、组织部、宣传部 |
| 行政部门 | 26 | 校长办、人力处、科研院、研究生院 |
| 教学科研单位 | 56 | 全部学院、研究所、研究中心 |
| 医学院 | 7 | 医学院 + 6 所附属医院 |
| 公共服务单位 | 15 | 图书馆、档案馆、医院、超算中心 |
| 专题网站 | 4 | 信息公开、人才招聘、招生 |
| 搜索门户 | 1 | 智搜门户 search.nju.edu.cn |

---

## 8. 常见问题

### Q1：独立爬虫模块 HTTP 模式能爬取所有页面吗？

不能。HTTP 模式只能爬取纯静态页面。部分动态页面（如课程表系统、需要 JavaScript 渲染的页面）需要使用浏览器模式。

### Q2：browser-start.js 报错"未找到浏览器服务脚本"？

请确保：
1. 已解压 `浏览器服务_skill.zip`
2. `NJU_BROWSER_SKILLS` 环境变量指向解压目录

### Q3：端口 4100 被占用？

```cmd
curl -X POST http://127.0.0.1:4100/shutdown
```
或重启计算机（最彻底）。

### Q4：如何查看采集了多少数据？

独立爬虫：`node standalone/collector.js --stats`
完整系统：`node -e "console.log(require('./src/storage').countRecords())"`

### Q5：Agent 如何接入真实 LLM？

```cmd
set ANTHROPIC_API_KEY=your_key_here
set LLM_MODE=claude
node src\agent\index.js
```

### Q6：python-docx 未安装？

```bash
pip install python-docx
```
