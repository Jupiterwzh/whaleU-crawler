[project]
name = "whalequery"
version = "0.1.0"
description = "南京大学通知公告多 Agent 协作系统（Docker 分发）"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0",
    "pyyaml>=6.0",
    "httpx>=0.27",
    "python-dotenv>=1.0",
]

[tool.pytest.ini_options]
testpaths = ["explorer-agent/tests"]
