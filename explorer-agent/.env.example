# LLM 配置（CherryIN deepseek）— 复制为 .env
# 注意：值含特殊字符（如括号）时必须加双引号
# main.py 启动时自动加载本文件（python-dotenv），无需手动 source
# LLM_API_KEY 通过 ${CHERRYIN_API_KEY} 引用 WSL 环境变量
LLM_BASE_URL="https://open.cherryin.cc/v1"
LLM_API_KEY=${CHERRYIN_API_KEY}
LLM_MODEL="deepseek/deepseek-v4-flash(free)"

# 路径配置（绝不硬编码，按你的实际路径填写）
STRATEGIES_DIR="<你的项目路径>/crawler/data/strategies"
CRAWLER_SCRIPT="<你的项目路径>/crawler/src/collectors/collector.js"
NJU_BROWSER_DIR="<项目路径>/nju-browser"
# DATA_DIR 不填则自动从 STRATEGIES_DIR 推导（取上级目录）
