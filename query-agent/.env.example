# 本 Agent 独立配置（可选）— 复制为 .env
#
# 本项目用「根目录集中配置」：公共配置在项目根 .env（LLM key/模型/URL）。
# 本文件只放「想独立于根」的键；不写的键自动继承根 .env。
# 例如想给本 Agent 单独指定 key，就写：
#   LLM_API_KEY=你的独立key
#
# 路径无需配置：STRATEGIES_DIR/CRAWLER_SCRIPT/RAG_DIR 等留空自动推导到项目内标准位置。
