# .gitlab-ci.yml — 每次 push 自动跑单元测试
stages:
  - test

unit-test:
  stage: test
  image: python:3.11
  before_script:
    - pip install -r explorer-agent/requirements.txt
  script:
    - cd explorer-agent && python -m pytest -q
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push"'
