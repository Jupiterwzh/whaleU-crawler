# 环境与密钥
.env
*.env

# Python
__pycache__/
*.pyc
.pytest_cache/

# Agent 运行产物
explorer-agent/traces/
query-agent/traces/

# RAG / 备份运行产物（Agent 运行时生成，非源码）
# 注：RAG 数据（data/rag/、crawler/data/rag/）作为示例数据提交（作业允许），仅备份不提交
crawler/data/backups/
data/backups/

# 旧 src 暂留（仅根目录，不影响 explorer-agent/src）
/src/

# 临时
*.bak

# 用户阅读/学习文档（教程等，不跟踪）
无关文档/

# 本地运行时配置（含个人绝对路径），仅用 .example 模板
opencode.json

# Superpowers SDD 内部工作产物
.superpowers/
