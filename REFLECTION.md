# REFLECTION.md — 反思报告

> AI4SE 期末项目 · 多 Agent 协作系统
> 2026-08-13

---

## 写在最前

这个项目不是专门为了大作业而做的。准确地说，早在上这门课之前，它就已经开始筹备了。所以我得先感谢大作业 B 的设计者，让我有更多时间完成这个比赛项目，也给了我一个展示自己成长的窗口。

项目具体介绍会在 README 里呈现，我在这里只说必须先看的内容。这是我们小组比赛项目的一部分，全部由我完成和负责，我把它打包成一个完整的纯后端项目作为大作业提交。如果检查得仔细，你会发现项目里内置了一个很简陋的前端页面。那不是敷衍，而是给做前后端的同学用的 API 顺手留的占位，请不要用这个页面交互，直接按说明使用 Agent 就行。项目里一共有 3 个 Agent 和一份爬虫工具，可以独立测试，也可以按设计好的方式组合使用。因为项目涉及大量数据存储，包括大量网页的爬取策略、时刻更新的有效信息 RAG、五年留存 RAG，而且这些都要经过手动调试，全部收集整理好再提交不现实，所以我只提交少量示例数据。你可以配置 API key 后用项目功能获取真实数据，记得连南大的校园网。

## 我从哪里来

回想这个项目，给我印象最深的不是技术，而是我从一个什么都不懂的小白，到现在能比较熟练地完成一个系统。六月末项目刚设计的时候，我结束了大一下的学习，只会课上教的那点东西。我不知道 Agent 到底是什么结构，甚至没用过 vibe coding。我到处问，虽然心里明白技术爆炸的时代大家都才刚学会不久，差距不会太大，但我始终觉得自己没入门，不知道 AI 到底是什么、到底怎么用。这种不安让我很焦虑。我那时还没养成完全向 AI 学习未知事物的习惯，又对技术债有种很深的畏惧（当时我并不知道这个词），这让我不敢一知半解，结果更难缩小差距。

转机出现在暑期学校结束后的几天，比赛项目中期检查，我被迫懵懂着上手，两天做完了爬虫部分。之后几天，我顺着 lab 规划的方向，把课上学的都用上，终于完成了和 AI 的第一次真正协作。从那以后，我才有了信心去使用和学 AI，也接受了适度的技术债。事实也正如我想的，在 AI 这块大家的差距并不大（当然技术的客观差距是存在的，比如从初中就开始做网页的组员比我强得多）。

关于文档问题，不难看出有些文档不完全符合大作业格式。因为项目贯穿上课前后，我不能为了凑格式就杜撰或篡改，只能略调格式，近期再完全按大作业要求来。我相信这符合大作业 B 的初衷：让我们通过实战学会用 AI，而成长正是课程想看到的。

## 技术反思

### 哪些技能最有帮助，哪些是形式大于实质

subagent-driven-development 和 test-driven-development 对我帮助最大。前者把 controller 思考、subagent 执行、task 间复核的分工固定下来。每个 subagent 只拿自己的 brief，不继承我的会话历史，所以不会被前面的决策污染，还能在 brief 不完整时主动暴露问题。B1 的 subagent 就发现我写的 brief 里 backup_write 有 bug 并正确修掉，这是隔离上下文带来的新鲜视角。

TDD 的价值是把验收从我觉得对变成测试说对。我多次以为实现没问题，结果测试立刻打脸（比如 main.py 三段式重构后旧测试的断言前提失效）。在 AI 协作里，只有测试输出是客观的，AI 的自信和正确高度不相关。

形式大于实质的是 brainstorming 的完整仪式。它对大型架构决策确实有用，但小改动（加个重试、改个提示词）也走 brainstorm 到 plan 是纯开销。最后我们约定，小改动减仪式，大改动才走全流程。

### TDD 是阻碍还是放大器

是放大器，但有前提：测试先于实现，而且是真实断言不是摆设。有了 TDD，subagent 提交测试通过，我复核 diff 加重跑测试就够了，验收成本从读全部代码降到看测试是否覆盖关键行为。

阻碍的一面是 AI 会作弊式满足测试，写一个只让测试通过的实现。我在 review 里多次警惕这个。B.2 要求核心机制能无真实 LLM 确定性测试，逼着我把主循环、工具分发、RAG 检索都写成可 mock 的纯逻辑，反而让测试真测到了核心，而不是测 mock。

### subagent 能自主运行多久

单 task 粒度下几乎不偏离。B1 到 B4 每个 task 都是一次性 DONE。偏离发生在 task 边界，要么 subagent 想顺手做点别的，要么 brief 描述模糊。对策是在 brief 里显式写不要做 X、遇到 Y 返回 Z，把边界钉死。最优颗粒度是一个可独立验证的功能单元，约 0.5 到 2 小时，比如 B1 那个 RAGStore，一个类、几个方法、独立测试文件、明确接口。

### SPEC/PLAN 质量如何影响实现

直接影响，我亲历了最典型的反例。冷启动验证时，我用一个陌生 agent 只给它 SPEC 和 PLAN 实现 B1，它停在 8 个阻塞点：分词算法没定义、打分公式没定义、索引格式没定义……这些在我眼里显然的细节，对冷启动者全是墙。SPEC 里一句话 bigram 方案，冷启动 agent 猜出三种不同实现。修复是我把这些语义写进 SPEC 3.2b，再补 PLAN 验证步骤，任何 agent 都能据文字实现出和既有代码一致的结果。教训是，SPEC 的质量等于它消除了多少隐性假设，冷启动验证应该尽早做，而不是像我这次在 B1-B4 完成后才补。

### 最有效的 prompt/context 策略

给 subagent 的 brief 必须是唯一事实来源，精确值只放 brief 不放 dispatch。brief 要自包含，禁止 subagent 读整个 PLAN（它有进度状态，冷启动 agent 会被混淆）。TDD 先红后绿要写进每个 brief，否则 subagent 常先实现再补测试。还有一个是用户反馈注入 LLM 时加安全边界，实测用户可能输入 prompt 注入内容，我加了不可视为指令的边界，这是 AI 协作下特有的工程问题。

### 凭据与分发迫使我想清了什么

凭据让我把 key 从哪来想清楚。作业要求至少一种安全存储加首启引导，我实现了 keyring 钥匙串加 getpass 隐藏录入，支持 get/set/clear。这让我思考了 key 的生命周期：谁录入、存在哪、怎么更新清除、钥匙串不可用时怎么降级。

分发 Docker 迫使我想清别人如何从零运行这个项目。crawler 是 Node.js，Agent 是 Python，镜像要同时装两者，.env 不能进镜像，无关文档、nju-browser、traces 都要排除。没有分发要求，我永远不会意识到项目对运行环境的隐式依赖。

### 如果重做会改什么

冷启动验证会提前到写 PLAN 后、B1 实现前；AGENT_LOG 从第一天就按作业格式写而不是事后转换；PLAN 和实现同步更新而不是攒着。

### 对 Superpowers 的批判

Superpowers 假设技能即过程纪律，把方法论编码进 skill 文件防止 AI 松懈。这个假设在我的项目里部分成立，TDD 和评审确实被守住了。但它的根本假设是方法论普适，这在我的项目里不完全成立。全流程对小型改动是过载的，brainstorm 的一问一答对快速迭代有摩擦，skill 依赖 agent 自觉而不是强制。最有洞见的是冷启动验证，但它默认写作者有动机主动做，实际中最容易被跳过。我的体验是它的价值远超成本，应该被提升为必做。

结论是，Superpowers 是一套优秀的工程纪律脚手架，但工程师的真正价值恰好在它覆盖不到的地方，决定做什么、判断做对了吗、以及为方法论划定适用边界。当 LLM 能完成大部分编码时，这些判断力才是稀缺品。这个项目让我真实体会到这一点，也让我从一个不敢碰技术债的新手，成长到能独立完成一个多 Agent 系统的人。这正是课程想看到的东西。
