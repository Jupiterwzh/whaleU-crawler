# REFLECTION.md — 反思报告

> 有感而发，经AI润色

---

## 写在最前

这个项目不是专门为了大作业而做的。准确地说，早在上这门课之前，它就已经开始筹备了。所以我得先感谢大作业 B 的设计者，让我有更多时间完成这个比赛项目，也给了我一个展示自己成长的窗口。

项目具体介绍会在 README 里呈现，我在这里只说必须先看的内容。这是我们小组比赛项目的一部分，全部由我完成和负责，我把它打包成一个完整的纯后端项目作为大作业提交。如果检查得仔细，你会发现项目里内置了一个很简陋的前端页面。那不是敷衍，而是给做前后端的同学用的 API 顺手留的占位，请不要用这个页面交互，直接按说明使用 Agent 就行。项目里一共有 3 个 Agent 和一份爬虫工具，可以独立测试，也可以按设计好的方式组合使用。因为项目涉及大量数据存储，包括大量网页的爬取策略、时刻更新的有效信息 RAG、五年留存 RAG，而且这些都要经过手动调试，全部收集整理好再提交不现实，所以我只提交少量示例数据。你可以配置 API key 后用项目功能获取真实数据，记得连南大的校园网。

## 我的成长

回想这个项目，给我印象最深的不是技术，而是我从一个什么都不懂的小白，到现在能比较熟练地完成一个项目的成长历程。六月末项目刚设计的时候，我结束了大一下的学习，只会课上教的那点东西。我不知道 Agent 到底是什么结构，甚至没用过 vibe coding。我到处问，虽然心里明白技术爆炸的时代大家都才刚学会不久，差距不会太大，但我始终觉得自己没入门，不知道 AI 到底是什么、到底怎么用。这种不安让我很焦虑。我那时还没养成完全向 AI 学习未知事物的习惯，又对技术债有种很深的畏惧（即使当时我并不知道这个词），这让我不敢一知半解，结果更难缩小差距。

转机出现在暑期学校结束后的几天，比赛项目中期检查，我被迫懵懂着上手，两天做完了爬虫部分。之后几天，我顺着 lab 规划的方向，把课上学的都用上，终于完成了和 AI 的第一次真正协作。从那以后，我才有了信心去使用和学 AI，也接受了适度的技术债。事实也正如我想的，在 AI 这块大家的差距并不大（当然技术的客观差距是存在的，比如从初中就开始做网页的组员比我强得多）。

关于文档问题，不难看出有些文档不完全符合大作业格式。因为项目贯穿上课前后，我不能为了凑格式就杜撰或篡改，只能略调格式，近期再完全按大作业要求来。我相信这符合大作业 B 的初衷：让我们通过实战学会用 AI，而成长正是课程想看到的。

## 技术反思

### 哪些技能最有帮助，哪些是形式大于实质

subagent-driven-development 和 test-driven-development 对我帮助最大。前者的好处是每个 subagent 只拿到自己的 brief，不继承我的会话历史，所以能保持新鲜视角，brief 不完整时还会主动暴露问题。我印象很深的是 B1 的 subagent 发现我 brief 里 backup_write 写错了，自己修好还告诉了我原因。

TDD 的价值是把验收从我觉得对变成测试说对。我几次以为实现没问题，一跑测试就被打脸，比如 main.py 重构后旧测试的断言前提变了。在 AI 协作里，只有测试输出是客观的。

形式大于实质的是 brainstorming 的完整流程。它对大架构决策有用，但小改动也走一遍就太啰嗦了，后来我们约定小改动直接做，大改动才走全流程。

### TDD 是阻碍还是放大器

是放大器，但有个前提：测试要先写、还要是真实断言。有 TDD 时，subagent 提交测试通过，我复核 diff 重跑测试就行，验收从读全部代码变成看测试覆盖。

阻碍的一面是 AI 会想办法让测试通过，比如只写满足断言的实现。B.2 要求核心机制能无真实 LLM 测试，这反而逼我把主循环、工具分发、RAG 检索写成可 mock 的纯逻辑，测试也就真测到了核心。

### subagent 能自主运行多久

单 task 粒度下基本不跑偏，B1 到 B4 每个 task 都是一次性完成。偏离多发生在 task 边界，要么 subagent 想顺手多做点，要么 brief 没说清。对策是在 brief 里把边界写死，明确做什么、不做什么。最优颗粒度是一个能独立验证的功能单元，大约 0.5 到 2 小时。

### SPEC/PLAN 质量如何影响实现

直接影响，我亲历过最典型的例子。冷启动验证时，我让一个陌生 agent 只拿 SPEC 和 PLAN 实现 B1，它停在好几个阻塞点：分词算法没写清、打分公式没写清、索引格式没写清。这些在我眼里是显然的，但对它全是墙。SPEC 里一句 bigram 方案，它猜出了三种不同实现。后来我把语义写细进 SPEC，再补验证步骤，任何 agent 照着文字就能实现出一样的结果。教训是，SPEC 的质量等于它消除了多少我没说出口的假设，冷启动验证应该早点做。

### 最有效的 prompt/context 策略

给 subagent 的 brief 必须是唯一事实来源，精确值只放 brief。brief 要自包含，别让 subagent 读整个 PLAN，不然它会被进度状态搞混。TDD 先红后绿要写进每个 brief，否则 subagent 常先实现再补测试。还有一个是用户反馈注入 LLM 时要加安全边界，防止 prompt 注入，这是 AI 协作下特有的问题。

### 凭据与分发迫使我想清了什么

凭据让我把 key 从哪来想清楚。作业要求安全存储加首启引导，我做了 keyring 加隐藏录入，支持查看、更新、清除。这让我开始想 key 的生命周期：谁录入、存哪、怎么改、钥匙串不可用时怎么办。

分发 Docker 让我想清别人怎么从零跑这个项目。爬虫是 Node.js，Agent 是 Python，镜像要装两者，.env 不能进镜像，本地参考文档、浏览器目录、轨迹都要排除。没有分发要求，我可能一直注意不到项目对运行环境的隐式依赖。

### 如果重做会改什么

冷启动验证会提前；AGENT_LOG 从第一天就按作业格式写，而不是事后补；PLAN 和实现同步更新，而不是攒到后面。

### 对 Superpowers 的批判

Superpowers 把方法论写进 skill 文件，想靠流程纪律防住 AI 松懈。这个假设在我项目里部分成立，TDD 和评审确实被守住了。但它的前提是方法论普适，这在我项目里不完全成立。全流程对小改动过重，brainstorm 的一问一答对快速迭代有摩擦，skill 也依赖 agent 自觉而不是强制。最有洞见的是冷启动验证，但它容易被跳过。我的体会是它价值远超成本，应该列为必做。

结论是，Superpowers 是一套很好的工程纪律脚手架，但工程师的真正价值恰好在它覆盖不到的地方：决定做什么、判断做对了吗、为方法论划定边界。这个项目让我真正体会到这一点，也让我从一个不敢碰技术债的新手，长成能独立完成一个多 Agent 系统的人。这正是课程想看到的。
