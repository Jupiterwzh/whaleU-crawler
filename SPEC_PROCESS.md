# SPEC_PROCESS.md — 规约协作过程记录

> 记录与 AI 协作生成 SPEC/PLAN 的过程，以及冷启动验证结果。
> 更新：2026-08-12

---

## 1. 项目背景与需求演变

本项目最初是"南京大学通知公告爬虫 + 策略生成 Agent"（session 1-7，A 组实现）。作为 AI4SE 期末大作业（B 类应用项目，含自研 agent 部分）提交时，核心目标升级为：

> **基于 harness 工程的多 Agent 协作系统**：策略 Agent 生成爬取策略 → 爬虫抓取 → RAG 存储检索 → 查询 Agent 用自然语言回答，RAG 数据不足时自动调爬虫补充。

这个升级由用户明确（2026-08-12）：在现有 crawler + explorer-agent 基础上，增加 RAG 存储层 + 查询/编排 Agent。

---

## 2. Brainstorm 关键迭代（≥3 轮）

### 迭代 1：RAG 实现方式（4 问之一）

**我的提问**：RAG 存储层用哪种实现？自研轻量检索 / 向量 embedding / 混合？

**用户回答**：RAG 用于大型项目信息获取，要求至少半小时更新一次，存储爬虫全部内容，计划用两个 RAG（有效内容 + 五年内全部）。

**我的处理**：基于此需求推荐自研倒排索引方案——两个 RAG 共享同一 doc 库，仅索引过滤条件不同（current 只含 valid 且未过期；archive 含全部），避免数据复制。

**用户修正**：① 不要为满足大作业要求额外约束设计；② 30 分钟是未来可能的默认频率，不固定，应可配置更短。

**我的调整**：`refresh_interval_min` 做成配置项，不硬编码；B.2 测试要求不反向限制选型。

### 迭代 2：查询 Agent 架构（4 问之二）

**我的提问**：查询 Agent 是复用 harness 新增入口，还是独立第二个 Agent，或 Boss Agent 调度？

**用户选择**：复用 harness，新增入口。

**我的处理**：确认 query-agent 复用 AgentLoop/guardrail/tracer，装配不同工具集。这是关键决策——避免重复实现 harness，且天然满足 B.2"主循环/工具分发/治理护栏自研"（explorer-agent 已自研，query-agent 复用）。

### 迭代 3：查询 Agent 工具集（4 问之三）

**我的提问**：查询 Agent 需要哪些工具？

**用户选择**：RAG+爬虫+文件工具（rag_search + run_crawler + read_file + run_shell）。

**我的处理**：确认工具集，"先搜→不够→爬→再搜→回答"的自主任决策略。未采纳"再加策略生成工具"选项——模式 A 已支持该能力，避免重复。

### 迭代 4：用户交互界面（4 问之四）

**我的提问**：CLI 问答 / Web 问答 / CLI 优先预留 Web？

**用户选择**：CLI 优先，预留 Web。

**我的处理**：MVP 只做 CLI（query.py），接口预留扩展点，不引入 Web 框架依赖。

---

## 3. 我采纳的 AI 建议 vs 我推翻的

**采纳的 AI 建议**：
1. **复用 harness 而非新建**——避免重复实现主循环/护栏，代码共享。
2. **双 RAG 共享 doc 库，仅索引过滤不同**——避免数据复制，存储高效。
3. **refresh_interval 配置化**——满足"频率可变"的真实需求。
4. **run_crawler 失败回退已有 RAG**——健壮性。

**我推翻/修正的 AI 建议**：
1. **"为满足 B.2 确定性测试而用自研索引"**——用户指出不应让作业要求反推设计，检索方案应从真实需求出发。最终保留自研方案（因为确实轻量且够用），但动机改为工程合理，而非应试。
2. **"固定 30 分钟刷新"**——用户纠正为可配置频率。
3. **"查询 Agent 直接调策略 Agent"**——改为直接调爬虫（有策略就用，无策略走模式 A 已有逻辑），避免 Agent 间递归调用。

---

## 4. 冷启动验证（客观证据）

### 执行方式

- **agent 类型**：`explore`（与主开发的 `general` 类型不同）
- **session**：全新，无历史/memory
- **输入**：仅 `SPEC.md` + `PLAN.md`
- **任务**：实现 Task B1（RAGStore）
- **规则**：不确定即暂停询问，不猜测
- **时间点说明**：本验证在 B1-B4 主体实现完成后补做（**事后验证**），并非"正式实现前"。如实标注：作业要求"实现前"做冷启动，本项目的执行顺序未严格满足；但冷启动的核心价值——用陌生 agent 暴露 spec 隐性假设、检验 spec 是否"文字写清楚"——**已真实发生并产生 8 处 spec 缺陷修订**（见下）。即"验证本身有效"，仅时间点偏后。这是真实过程，未伪造成"实现前"。

### 验证结果

**冷启动 agent 停在 B1 的实现细节**，列出 8 个阻塞级 + 7 个次级不明点，核心：

| # | 冷启动发现的不明点 | 处理 |
|---|-------------------|------|
| Q1 | 分词算法未定义（bigram 的 CJK/ASCII/大小写规则） | SPEC §3.2b 明确 |
| Q2 | 打分公式未定义 | SPEC §3.2b 明确（tf×idf） |
| Q3 | 索引磁盘格式未定义 | SPEC §3.2b 明确（单文件 index.json） |
| Q4 | current/archive 流转与检索语义未定义 | SPEC §3.2b 明确（仅 current 参与 search） |
| Q5 | 分片(slice)定义未定义 | SPEC §3.2b 明确（= 一个 domain.date.jsonl 文件） |
| Q6 | dedup_hash 归属与策略未定义 | SPEC §3.2b 明确（ingest 内 sha256(url|title)，重复丢弃） |
| Q7 | crawler→RAG 归一化无归属 | SPEC §3.2b 明确（归 ingest） |
| Q8 | meta 缺失时首启行为未定义 | SPEC §3.2b 明确（is_stale 返回 True） |

**与既有实现（commit `8ea830a`）的出入**：冷启动 agent 对分词/打分/索引格式的猜测，与 B1 实现（subagent 做的 bigram 分词、tf×idf、单文件 index.json、`valid` 缺失默认 True）**高度一致**——说明实现符合直觉，但 SPEC 文字未固化这些决策。

**关于"不一致解读是 spec 错还是 agent 读错"**：Q1-Q8 的不明点全部属于 **spec 未写清**（分词规则、打分公式、索引格式、current/archive 语义、slice 定义、dedup 归属、归一化归属、首启行为均未在 SPEC 明文定义），**不是 agent 读错**——agent 的猜测方向与既有实现一致，只是 SPEC 缺少权威文字导致它无法确认。即"spec 写错（缺定义）"，非"agent 理解错"。

**产出与预期差距**：冷启动 agent 未产出可运行的实现（停在 B1 的 8 个不明点），产出与"完成任务"差距大；但作为 **spec 质量检验**，其价值完全达成（暴露 15 个不明点并推动 §3.2b 补全）。

### 据此做的 SPEC/PLAN 修订

**SPEC.md 新增 §3.2b（RAG 详细规约）**，将 Q1-Q15 全部写死为权威定义：
- 分片 = `docs/<domain>.<date>.jsonl`；slice 标识 = `<domain>.<date>`
- bigram 分词规则 + 2 个示例
- tf×idf 打分公式 + 返回 schema `{title,url,date,content,domain,score}`
- 索引落盘格式 `{"terms","docs"}`
- current/archive 流转：仅 current 参与检索
- dedup_hash = `sha256(url|title)`，重复丢弃
- crawler→RAG 归一化归 ingest
- meta 缺失 → is_stale 返回 True

**PLAN.md B1 补充验证步骤**（含失败测试判据）：空库返回 []、命中、去重、stale 阈值、重建后可检索。

**修订 diff 要点**：SPEC 从"bigram 方案"一句话 → §3.2b 一整节具体规约；PLAN B1 从"4 测试通过"注记 → 接口 + 权威语义引用 + 验证清单。

---

## 5. 反思：brainstorm 与冷启动的优缺

**做得好的**：
- 4 轮一问一答明确了关键架构决策（RAG 方案/Agent 架构/工具集/界面），没有在一开始就大段铺开设计图。
- 冷启动验证确实暴露了 spec 的"隐形假设"——我作为主 agent 与用户共担了太多隐性上下文（如"bigram 怎么实现"这种我以为显然的东西，对冷启动 agent 是阻塞点）。

**让我不满的**：
- brainstorm 阶段我在"RAG 实现方式"问题上过早给出自研方案推荐，差点把"为 B.2 应试"混入设计动机，被用户纠正。
- SPEC 初稿的 §3.2 太简略，"bigram 方案"一句话隐藏了大量决策；如果冷启动验证晚做，这些假设会永远留在"我以为写清楚了"的状态。

**教训**：SPEC 的"我以为写清楚了"≠"文字写清楚了"。冷启动验证是把隐性假设显性化的最有效手段，应尽早做（在 B1 实现之前），而非在 B1-B4 完成后补做（本次属于事后验证，价值仍在但打了折扣——若提前做，B1 的 spec 就能直接指导 subagent，而非靠 subagent 自己猜+事后补文档）。
