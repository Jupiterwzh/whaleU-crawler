# 项目需求：Cherry Agent Framework

## 一、项目概述

构建一个**模块化、可扩展的 AI Agent 框架**（Cherry Agent），实现「单 Agent 核心循环 + 多 Agent 协作」。框架采用 **Harness Engineering** 架构：LLM 只负责一行任务决策，其余全是工程零件（Harness）。

## 二、核心架构

```
┌─────────────────────────────────────────────────┐
│                   agent.yaml                      │
│               （声明式总配置）                      │
└────────────┬────────────────────────────────────┘
             │ 驱动装配
             ▼
┌─────────────────────────────────────────────────┐
│              Harness（零件容器）                    │
│  system_prompt │ rules │ skills │ tools │ mcp     │
│  hooks │ guardrail │ sandbox │ tracer │ memory   │
│  retriever                                       │
└────────────┬────────────────────────────────────┘
             │ 传入
             ▼
┌─────────────────────────────────────────────────┐
│           AgentLoop（核心循环）                     │
│  ① 上下文装配 → ② while 循环 → ③ 收尾            │
│  每轮: LLM 决策 → 门控 → 钩子 → 分发 → 回灌       │
└─────────────────────────────────────────────────┘
```

## 三、必须实现的功能模块

### 3.1 Harness 装配系统

| 需求编号 | 描述 | 优先级 |
|---------|------|--------|
| H-01 | 从 `agent.yaml` 声明式装配所有零件，生成 Harness 对象 | P0 |
| H-02 | 支持 `restrict(scope)` 方法，生成子 Agent 用的受限 Harness（工具子集、步数上限、不同模型） | P0 |
| H-03 | 配置热加载：修改 yaml 后无需重启即可生效（可选，P0 可接受重启） | P2 |

### 3.2 核心循环（AgentLoop）

| 需求编号 | 描述 | 优先级 |
|---------|------|--------|
| L-01 | 实现 while 循环：每轮一次 LLM 调用 + 动作分发 | P0 |
| L-02 | LLM 返回解析为 `{text, action}` 结构。action 支持类型：`done`、`call_tool`、`use_skill`、`spawn_subagent`、`take_note` | P0 |
| L-03 | 上下文管理：system_prompt + rules + memory.read + retriever → 开局装配；工具结果 → 循环内回灌 | P0 |
| L-04 | 熵管理：token 超阈值时自动 compact（调 LLM 压缩历史） | P1 |
| L-05 | 预算守卫：max_steps 上限、超时、重复调用检测 | P0 |

### 3.3 工具系统

| 需求编号 | 描述 | 优先级 |
|---------|------|--------|
| T-01 | 工具注册：每个工具 = `{name, description, parameters(JSON Schema), handler}` | P0 |
| T-02 | 内建工具至少包含：`read_file`、`write_file`、`run_shell` | P0 |
| T-03 | 工具描述自动转换为 OpenAI/Anthropic Function Calling 格式 | P0 |
| T-04 | 工具调用失败时，错误信息回灌到上下文（而非崩溃） | P0 |

### 3.4 MCP 集成

| 需求编号 | 描述 | 优先级 |
|---------|------|--------|
| M-01 | MCPHub：管理多个 MCP Server 的连接生命周期（启动/关闭） | P0 |
| M-02 | 自动拉取每个 Server 的工具列表，合并到「功能菜单」 | P0 |
| M-03 | MCP 工具调用走协议，与内建工具统一接口（调用方不关心工具来源） | P0 |
| M-04 | 支持 `.yaml` 配置 MCP Server（command + args + env） | P0 |

### 3.5 技能系统（Skill）

| 需求编号 | 描述 | 优先级 |
|---------|------|--------|
| S-01 | SkillRegistry：注册技能名片（name + description），不加载全文 | P0 |
| S-02 | LLM 通过 `use_skill` 动作命中技能时，才加载 SKILL.md 全文注入上下文 | P0 |
| S-03 | 技能名片出现在 LLM 功能菜单中（与工具并列） | P0 |
| S-04 | 支持 `skills/index.yaml` 注册多个技能 | P0 |

### 3.6 权限门控（Guardrail）

| 需求编号 | 描述 | 优先级 |
|---------|------|--------|
| G-01 | 动作执行前校验：支持 deny（拒绝）、ask_user（询问用户）、allow（放行）三种策略 | P0 |
| G-02 | 策略通过 `guardrails/policy.yaml` 声明式配置 | P0 |
| G-03 | 拦截原因回灌到上下文，让 LLM 知道为什么被拦 | P0 |
| G-04 | 危险操作（`rm -rf`、`sudo`、对外 POST 请求等）默认至少为 ask_user | P1 |

### 3.7 钩子系统（Hooks）

| 需求编号 | 描述 | 优先级 |
|---------|------|--------|
| HK-01 | 支持三种生命周期事件：`PreToolUse`、`PostToolUse`、`SessionEnd` | P0 |
| HK-02 | PreToolUse 可返回 deny 拦截（与 Guardrail 互补） | P0 |
| HK-03 | PostToolUse 可修改结果（如截断过大的输出） | P1 |
| HK-04 | 钩子通过 Python 模块注册，路径在 agent.yaml 中配置 | P0 |

### 3.8 沙箱

| 需求编号 | 描述 | 优先级 |
|---------|------|--------|
| SB-01 | 支持 Docker 沙箱：工具在隔离容器中执行 | P1 |
| SB-02 | 支持本地沙箱（开发模式，无隔离） | P0 |
| SB-03 | 资源限制：memory_limit、cpu_limit、timeout | P1 |
| SB-04 | 网络策略：默认断网，仅白名单工具可出站 | P2 |

### 3.9 记忆系统

| 需求编号 | 描述 | 优先级 |
|---------|------|--------|
| MEM-01 | MemoryStore：基于 ChromaDB 的长期记忆 | P1 |
| MEM-02 | `read(goal)` — 开局检索相关记忆，注入上下文 | P1 |
| MEM-03 | `write(note)` — 循环内 LLM 通过 take_note 动作主动记录 | P1 |
| MEM-04 | `consolidate(context)` — 会话末将零散笔记固化为长期记忆 | P2 |

### 3.10 RAG 检索器

| 需求编号 | 描述 | 优先级 |
|---------|------|--------|
| R-01 | Retriever：开局预检索相关知识，注入上下文 | P2 |
| R-02 | 支持 ChromaDB 作为后端 | P2 |

### 3.11 可观测性

| 需求编号 | 描述 | 优先级 |
|---------|------|--------|
| O-01 | Tracer：每轮记录决策和观察（jsonl 格式） | P1 |
| O-02 | `flush()` — 会话末落盘 | P1 |
| O-03 | 轨迹包含：trace_id、step、timestamp、text、action、observation | P1 |

### 3.12 多 Agent 协作

| 需求编号 | 描述 | 优先级 |
|---------|------|--------|
| MA-01 | `spawn_subagent` 动作：开独立上下文递归调用 AgentLoop | P0 |
| MA-02 | 子 Agent 使用受限 Harness（工具子集、步数上限） | P0 |
| MA-03 | MAX_DEPTH 守卫：防递归过深 / fork 炸弹 | P0 |
| MA-04 | 子 Agent 只返回摘要，不污染主上下文 | P0 |
| MA-05 | 支持并行 spawn 多个子 Agent（fan-out） | P1 |

### 3.13 反馈闭环

| 需求编号 | 描述 | 优先级 |
|---------|------|--------|
| FB-01 | 代码变更后自动运行传感器（Lint + 测试） | P2 |
| FB-02 | 传感器结果回灌到上下文，LLM 据此自我修正 | P2 |

## 四、技术约束

| 项目 | 要求 |
|------|------|
| 语言 | Python ≥ 3.11 |
| LLM 后端 | 支持 OpenAI API 兼容接口（gpt-4o、claude-sonnet-4 等均可） |
| 配置格式 | YAML |
| 向量数据库 | ChromaDB（本地模式） |
| 沙箱 | Docker（生产）/ 本地进程（开发） |
| MCP | 支持 stdio 传输的 MCP Server |
| 测试框架 | pytest |

## 五、交付物

```
cherry-agent/
├── agent.yaml                    # 声明式总配置（可直接运行）
├── rules/
│   └── AGENTS.md                 # 默认行为约束
├── skills/
│   ├── index.yaml                # 技能注册表
│   └── code-review/
│       └── SKILL.md              # 示例技能
├── guardrails/
│   └── policy.yaml               # 门控策略
├── src/
│   ├── harness.py                # Harness 类 + from_yaml
│   ├── agent_loop.py             # AgentLoop 核心循环
│   ├── tools/
│   │   ├── __init__.py
│   │   ├── registry.py           # Tool 对象 + 注册
│   │   ├── file_tools.py         # read_file / write_file
│   │   └── shell_tools.py        # run_shell
│   ├── mcp/
│   │   └── hub.py                # MCPHub + MCPServerConnection
│   ├── skills/
│   │   └── registry.py           # SkillRegistry
│   ├── hooks/
│   │   └── runner.py             # HookRunner
│   ├── guardrails/
│   │   └── guardrail.py          # Guardrail
│   ├── sandbox/
│   │   ├── base.py               # Sandbox 抽象接口
│   │   ├── local.py              # LocalSandbox
│   │   └── docker.py             # DockerSandbox
│   ├── memory/
│   │   └── chroma_store.py       # MemoryStore
│   ├── tracer/
│   │   └── tracer.py             # Tracer
│   └── llm/
│       └── client.py             # LLM 客户端封装
├── tests/
│   ├── test_agent_loop.py
│   ├── test_harness.py
│   ├── test_tools.py
│   └── test_multi_agent.py
├── main.py                       # 入口
├── README.md                     # 使用说明
└── requirements.txt
```

## 六、验收标准

1. **单 Agent 闭环**：输入「用 web_search 搜索 Python 最新版本，然后写入文件 result.txt」，Agent 能自主完成搜索 → 写文件 → 报告完成。

2. **门控生效**：尝试执行 `rm -rf /` 时，Guardrail 拦截并回灌原因。

3. **MCP 集成**：连接一个 MCP Server（如 GitHub），Agent 能列出 MCP 工具并成功调用。

4. **子 Agent 协作**：输入「先搜索 AI Agent 的资料，然后写一篇介绍文章」，系统 spawn 子 Agent 分阶段执行。

5. **技能渐进式加载**：Agent 的初始菜单中只有技能的一行描述。用户说「帮我 review 代码」时，Agent 触发 `use_skill("code-review")`，此时才加载 SKILL.md 全文。

6. **轨迹可审计**：每次运行后在 `traces/` 目录生成完整的 jsonl 轨迹文件。

## 七、开发优先级

| 阶段 | 范围 | 预期成果 |
|------|------|---------|
| **Phase 1 (MVP)** | Harness + AgentLoop + Tool + Guardrail + 入口 | 能跑单 Agent 循环 |
| **Phase 2** | MCP + Skill + Hooks + Sandbox(Local) | 工具生态 + 技能系统可用 |
| **Phase 3** | SubAgent + Memory + Tracer | 多 Agent 协作 + 可观测 |
| **Phase 4** | Docker Sandbox + Feedback Loop + RAG | 生产级安全 + 自修正 |
