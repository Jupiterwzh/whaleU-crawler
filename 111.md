# 111.md — 你（用户）需要手动做的事

> 生成时间：2026-08-13
> 这些是需要人工判断/验证/环境配置的事，AI 无法替代。按优先级排序。

---

## 一、策略 Agent 相关（需要你的判断）

### 1. 逐站点补充经验库（经验补充）

每探索一个站点，Agent 会输出 `【经验草案】`，你确认 y 后它合并进 `explorer-agent/experiences.json`。

**教程**：
```bash
cd /home/wangzhiheng/whaleU-crawler/explorer-agent
python main.py "探索 https://<站点>.nju.edu.cn/ 的通知公告入口"
# Agent 探索 → 验证 → 输出结果 + 经验草案
# 确认? (y/调整建议): y
# 是否将本次发现的通用规律存入经验库？（y/否）: y   ← 发现新规律时按 y
```

**要做的站点**（docx 清单 57 站中已测可爬的）：
- 研究生招生 `yzb.nju.edu.cn` ✅ 已完成
- 人力资源 `hr.nju.edu.cn`（首页 32 条）
- 统战部 `tzb.nju.edu.cn`（20 条）
- 研究生院 `grawww.nju.edu.cn`（注意首页是公众号外链）
- 其他你关心的部门站点

**注意**：已有策略的站点（cs/jw/yzb）不重复探索，直接爬虫用。

### 2. 核对域名异常站点

docx 清单里有几个域名可能标错：
- `jwb.nju.edu.cn` → 实际显示"纪检监察网"
- `xkb.nju.edu.cn` → 实际显示"学科建设与发展规划办公室"

**教程**：浏览器打开确认真实域名，把正确的加进你的站点清单。

---

## 二、爬虫相关（需要你的决策）

### 3. 外链甄别（grawww 暴露的问题）

`grawww.nju.edu.cn` 首页通知多是 `mp.weixin.qq.com` 公众号外链。需要决定：
- **保留外链**入库 RAG（外链也能回答"最近有什么通知"，但来源是微信）
- **过滤外链**，只收站内通知（更干净但可能漏掉通过公众号发的通知）

**待你决定后**，我改爬虫 `extractNotices` 加过滤逻辑。

### 4. ndwy.nju.edu.cn 需 VPN

`ndwy.nju.edu.cn`（物联网学院）需要南大 VPN 才能访问：
- 连接 VPN 时 Agent 进程可能无法访问（进程不走 VPN 路由）
- 不连 VPN 时站点不可达

**待你决定**：
- 给 Agent 进程配 VPN（需要了解你的 VPN 方案）
- 或跳过此类需 VPN 的站点
- 或你在已连 VPN 的终端手动跑

### 5. 给站点清单补充更多站点（可选）

docx 有 57 个 URL，我测试了 10 个。你可以挑其他要关注的站点，逐个用 `python main.py "探索 <url>"` 生成策略。

---

## 三、大作业工程收尾（需要你的操作）

### 6. WebUI 部署

WebUI 已实现（`query-agent/webui.py`），未部署。

**教程**：
```bash
cd /home/wangzhiheng/whaleU-crawler/query-agent
python webui.py 8000
# 浏览器打开 http://localhost:8000
```
要对外可访问（作业要求），需部署到公网/内网可达地址（如云服务器、frp 内网穿透等）。

### 7. CI 实跑 + 末次 pass

`.gitlab-ci.yml` 已写好（unit-test job），需你 push 到 GitLab 触发。

**教程**：
```bash
cd /home/wangzhiheng/whaleU-crawler
git remote add origin <你的GitLab仓库URL>
git push -u origin main
# GitLab 会自动跑 unit-test job
# 确认最后一次 CI 是 pass 状态
```

### 8. Docker build/run 验证

`Dockerfile` 已写，需你手动构建验证（耗时）。

**教程**：
```bash
cd /home/wangzhiheng/whaleU-crawler
docker build -t whalequery .
docker run -v $PWD/query-agent/.env:/app/query-agent/.env -e DEEPSEEK_API_KEY=$DEEPSEEK_API_KEY whalequery query "计算机学院最近有什么通知"
```

### 9. 仓库推送 + PR 工作流

当前单分支 main。作业要求 PR 工作流 + commit 标注 subagent。

**待做**：如需多分支/PR，告诉我，我协助规划。至少把代码推到远程仓库（第 7 步）。

---

## 四、测试相关（可选）

### 10. 跑全量测试确认

```bash
cd /home/wangzhiheng/whaleU-crawler
make test   # 或分目录跑
# Python: explorer-agent 54 + query-agent 26 + rag-manager 3
# Node: crawler 5
```

---

## 总结优先级

| 优先级 | 事项 | 耗时 |
|--------|------|------|
| 高 | 7. CI 实跑 + push（作业硬性要求末次 pass） | 10 分钟 |
| 高 | 6. WebUI 部署（作业要求可访问 URL） | 1-2 小时 |
| 高 | 8. Docker 验证（作业分发要求） | 20 分钟 |
| 中 | 1. 逐站点探索 + 经验补充（hr/tzb/grawww 等） | 每个 5-10 分钟 |
| 中 | 3. 决定外链是否保留 | 5 分钟 |
| 中 | 4. 决定 ndwy VPN 方案 | 10 分钟 |
| 低 | 2. 核对域名异常 | 10 分钟 |
| 低 | 10. 跑全量测试 | 2 分钟 |
