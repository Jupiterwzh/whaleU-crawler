# 111.md — 明日行动清单

> 更新时间：2026-08-13
> 按顺序做。每项含教程、预期、耗时。

---

## 第一部分：网站结构遍历收尾（新功能验证）

### ✅ 已完成（先了解背景）
- 策略 Agent 新增 `crawl_structure` 工具（BFS 遍历网站返回结构树）+ rules 遍历算法
- 59+ tests 全绿

### 1. 验证 crawl_structure 真实效果（5 分钟）

```bash
cd /home/wangzhiheng/whaleU-crawler/explorer-agent
python -c "
import sys; sys.path.insert(0, '.')
from src.tools.structure_tools import crawl_structure
tree = crawl_structure('https://cs.nju.edu.cn/', max_depth=2, max_links=15)
print('节点数:', len(tree['nodes']))
for n in tree['nodes'][:20]:
    print(f'  [{n[\"type\"]}] {n[\"url\"][:55]}')
"
```
**预期**：看到 list/middle/detail 标记的结构树。

### 2. 决定：分类精度调优（5 分钟，需你判断）

**问题**：部分带 `list.htm` 后缀的页面被 `_classify_page` 判为 `middle`（如 `/1716/list.htm`、`/1684/list.htm`），不是 `list`。

**选择**（告诉我即可，我来改）：
- **A**：URL 含 `list` 即视为列表页（简单，但可能误判）
- **B**：保持现状，靠 `--verify` 兜底验证最终入口（保守）
- **C**：其他

### 3. 决定：遍历参数（2 分钟）

- 深度上限 `max_depth=4` 是否合适？
- 每页链接上限 `max_links=30` 是否合适？
- 结构树是否需要保存为文件（如 `crawler/data/structures/<domain>.json`）供复查，还是仅终端展示？

---

## 第二部分：策略 Agent 经验补充（需你判断）

### 4. 逐站点探索 + 经验补充（每个 5-10 分钟）

```bash
cd /home/wangzhiheng/whaleU-crawler/explorer-agent
python main.py "探索 https://<站点>.nju.edu.cn/ 的通知公告入口"
# Agent 遍历 → 验证 → 输出结构树 + 策略 + 经验草案
# 确认? (y/调整建议): y
# 是否将本次发现的通用规律存入经验库？（y/否）: y   ← 有新规律时按 y
```

**要做的站点**：
- 人力资源 `hr.nju.edu.cn`（首页 32 条）
- 统战部 `tzb.nju.edu.cn`（20 条）
- 研究生院 `grawww.nju.edu.cn`（注意首页是公众号外链）
- 其他你关心的部门站点

**注意**：已有策略的站点（cs/jw/yzb）不重复探索。

### 5. 核对域名异常站点（10 分钟）

docx 清单里几个域名可能标错：
- `jwb.nju.edu.cn` → 实际显示"纪检监察网"
- `xkb.nju.edu.cn` → 实际显示"学科建设与发展规划办公室"

**教程**：浏览器打开确认真实域名，加进你的站点清单。

---

## 第三部分：爬虫决策（需你决定）

### 6. 外链甄别（grawww 暴露的问题，5 分钟）

`grawww.nju.edu.cn` 首页通知多是 `mp.weixin.qq.com` 公众号外链。决定：
- **保留外链**入库 RAG（能回答"最近有什么通知"，但来源是微信）
- **过滤外链**，只收站内通知（更干净）

**你决定后**，我改爬虫 `extractNotices` 加过滤。

### 7. ndwy.nju.edu.cn 需 VPN（10 分钟）

`ndwy.nju.edu.cn`（物联网学院）需要南大 VPN：
- 连 VPN 时 Agent 进程可能无法访问
- 不连时站点不可达

**决定**：给 Agent 配 VPN / 跳过此类站点 / 你在已连 VPN 终端手动跑。

### 8. 补充更多站点（可选）

docx 有 57 个 URL，已测 10 个。挑其他要关注的，逐个探索生成策略。

---

## 第四部分：大作业工程收尾（需你操作）

### 9. CI 实跑 + 末次 pass（10 分钟，作业硬性要求）

```bash
cd /home/wangzhiheng/whaleU-crawler
git remote add origin <你的GitLab仓库URL>
git push -u origin main
# GitLab 自动跑 unit-test job，确认末次 pass
```

### 10. WebUI 部署（1-2 小时，作业要求可访问 URL）

```bash
cd /home/wangzhiheng/whaleU-crawler/query-agent
python webui.py 8000
# 本地 http://localhost:8000
# 对外：部署到公网/内网可达（云服务器、frp 等）
```

### 11. Docker 验证（20 分钟，作业分发要求）

```bash
cd /home/wangzhiheng/whaleU-crawler
docker build -t whalequery .
docker run -v $PWD/query-agent/.env:/app/query-agent/.env -e DEEPSEEK_API_KEY=$DEEPSEEK_API_KEY whalequery query "计算机学院最近有什么通知"
```

### 12. 仓库推送 + PR 工作流（可选）

当前单分支 main。作业要求 PR + commit 标注 subagent。如需多分支/PR，告诉我协助规划。

---

## 测试确认（可选，2 分钟）

```bash
cd /home/wangzhiheng/whaleU-crawler
make test   # 或分目录
# Python: explorer-agent 60 + query-agent 26 + rag-manager 3
# Node: crawler 5
```

---

## 优先级速查

| 顺序 | 事项 | 耗时 |
|------|------|------|
| 1 | 验证 crawl_structure | 5 分钟 |
| 2 | 决定分类精度 | 5 分钟 |
| 3 | 决定遍历参数 | 2 分钟 |
| 4 | 逐站点探索+经验 | 每个 5-10 分钟 |
| 5 | 核对域名异常 | 10 分钟 |
| 6 | 决定外链甄别 | 5 分钟 |
| 7 | 决定 ndwy VPN | 10 分钟 |
| 8 | 补充站点（可选） | — |
| 9 | **CI 实跑（硬性）** | 10 分钟 |
| 10 | **WebUI 部署（硬性）** | 1-2 小时 |
| 11 | **Docker 验证（硬性）** | 20 分钟 |
| 12 | 仓库推送/PR | — |
| 13 | 全量测试确认 | 2 分钟 |
