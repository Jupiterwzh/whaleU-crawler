# 111.md — 待办教程

> 更新：2026-08-13
> 已完成的事项已从本文件移除（详见 AGENT_LOG.md）。这里只留仍需你操作/判断的待办，每项含命令与预期。

---

## 一、策略 Agent：批量探索站点生成策略

对 57 个 docx 域名（已核对）逐个探索生成策略。Agent 会用结构遍历 + verify 闭环，输出结构树让你选择入口。

```bash
cd /home/wangzhiheng/whaleU-crawler/explorer-agent
python main.py "探索 https://<站点>.nju.edu.cn/ 的通知公告入口"
# Agent 遍历 → 展示编号清单 → 你选入口编号（回车跳过）
# 生成策略 → --verify 验证 → 确认(y) → 是否存经验(y/否)
```

**建议站点**（已测可爬）：hr、tzb、grawww（注意公众号外链已过滤）
**参考**：docx 清单在桌面，jwb 实际是纪检监察机构（非教务处）

---

## 二、大作业工程收尾（硬性）

### CI 实跑 + 末次 pass
```bash
cd /home/wangzhiheng/whaleU-crawler
git remote add origin <你的GitLab仓库URL>
git push -u origin main
# 确认 GitLab unit-test job 末次 pass
```

### WebUI 部署（作业要求可访问 URL）
```bash
cd /home/wangzhiheng/whaleU-crawler/query-agent
python webui.py 8000
# 本地 http://localhost:8000；对外需公网/内网可达（云服务器、frp 等）
```

### Docker 验证（分发要求）
```bash
cd /home/wangzhiheng/whaleU-crawler
docker build -t whalequery .
docker run -v $PWD/query-agent/.env:/app/query-agent/.env -e DEEPSEEK_API_KEY=$DEEPSEEK_API_KEY whalequery query "计算机学院最近有什么通知"
```

### 仓库推送 + PR（作业流程）
当前单分支 main，如需 PR 工作流告诉我协助规划。

### query-agent 真实验收
```bash
cd /home/wangzhiheng/whaleU-crawler/query-agent
python query.py "计算机学院最近有什么通知"
# 预期：rag_search 命中 RAG → 回答带 URL，不触发爬虫
```

---

## 三、环境

- ndwy（需 VPN）：你自行解决
- docx 清单 jwb 注释：改"纪检监察机构"（我已核对 NJU 官网 xybm.htm）

---

## 测试确认（可选）
```bash
cd /home/wangzhiheng/whaleU-crawler
# explorer 70 + query 26 + rag 3 + crawler 6 = 105 全绿
make test   # 或分目录跑
```
